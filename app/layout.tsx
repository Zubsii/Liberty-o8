import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navigation from "@/components/navigation"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Liberty Printing & Sports - Custom Jersey Printing & Sports Merchandise",
  description:
    "Premium custom jersey printing, personalized sports merchandise, and sportswear solutions. Empowering local teams and athletes with quality gear.",
  keywords: "custom jersey printing, sports merchandise, teamwear, sports gear, Bangladesh",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
